/* contains all our types, but we might split them in the future into different files if necessary */

export type Meal = {
    name:string;
    calories:number;
    protein:number;
    carbs:number;
    fat:number;
}